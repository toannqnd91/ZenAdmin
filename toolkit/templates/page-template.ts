/**
 * Page Templates
 */

export const indexPageTemplate = `// Index page template for {{EntityName}}`

export const createPageTemplate = `// Create page template for {{EntityName}}`

export const editPageTemplate = `// Edit page template for {{EntityName}}`

export const detailPageTemplate = `// Detail page template for {{EntityName}}`
